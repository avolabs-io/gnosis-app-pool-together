type PoolCardProps = {
  poolIndex: string;
  prizeValue: string;
  tokenSymbol: string;
  tokenImageUrl: string;
  userBalance: string;
  secondsRemaining: number;
  loading: boolean;
};

export default PoolCardProps;
